<!DOCTYPE html>
<html lang="en">
<head>
	{{> head }}
	<title>{{title}} | Show Tracker</title>
	<style>
        form {
            text-align: center;
        }

        input {
            display: block;
            margin: 5px auto;
            padding: 0 3px;
            width: 26vw;
            min-width: 220px;
            max-width: 310px;
            line-height: 30px;
        }

        .btn-info {
            margin: 10px auto;
            padding: 8px 40px;
        }
    </style>
</head>
<body>
	<div class="container">
		<form action="{{url}}" method="POST">
			<h1>{{title}}</h1>
			<input type="text" name="username" id="username" placeholder="Username" autofocus>
			<input type="password" name="password" id="password" placeholder="Password">
            {{#if showCode}}<input type="password" name="code" id="code" placeholder="Registration Code">{{/if}}
			<button type="submit" class="btn btn-info">{{buttonText}}</button>
		</form>
	</div>
</body>
</html>